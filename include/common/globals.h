#ifndef _GLOBALS_H
#define _GLOBALS_H

#include <cstdio>

typedef int Player;

namespace globals {
	const size_t BOARD_SIZE = 8;
	const size_t DEFAULT_WIN_H = 480;
	const size_t DEFAULT_WIN_W = 720;
}

#endif // _GLOBALS_H
